{
  "name": "@escalated-dev/plugin-marketplace",
  "version": "0.1.0",
  "description": "Plugin marketplace browser and installer — TypeScript SDK version",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@escalated-dev/plugin-sdk": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
